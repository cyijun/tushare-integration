INSERT INTO {{ db_name }}.{{ table_name }} (
    {%- for column in column_bindings  %}
    `{{ column.name }}`{%- if not loop.last %}, {% endif %}
    {%- endfor %}
)
VALUES (
    {%- for column in column_bindings  %}
    :{{ column.bind }}{%- if not loop.last %}, {% endif %}
    {%- endfor %}
)
